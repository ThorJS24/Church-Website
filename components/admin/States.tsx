// Re-exported from the design system — kept here so the existing
// '@/components/admin/States' import path across the admin panel keeps
// working without touching every call site.
export { LoadingState, EmptyState, ErrorState } from '@/components/ui/states';
